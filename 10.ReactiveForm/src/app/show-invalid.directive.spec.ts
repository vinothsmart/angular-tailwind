import { ShowInvalidDirective } from './show-invalid.directive';

describe('ShowInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
